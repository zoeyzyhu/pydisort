name: Main CI Run

on:
  push:
    branches: [main]
    tags: [v*]

jobs:
  run-ci:
    uses: .github/workflows/ci.yml
    with:
      some-input: value
    secrets: inherit  # if you need access to secrets
